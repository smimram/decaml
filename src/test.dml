let n = type

let u = ()

let ten = 10

let id_nat (x : nat) = x

(* let id_nat : nat -> nat = fun x -> x *)

let id (a : type) (x : a) = x

let n = id nat 2

let n = id _ 2

let id {a : type} (x : a) = x

let n = id 2

(*
Local Variables: 
mode: tuareg
End:
*)
